export interface GuideCategory {
  id: number;
  title: string;
  subtitle: string;
  icon: string;
  color: string;
  bgColor: string;
}

export interface EmergencyContact {
  title: string;
  subtitle: string;
  number: string;
  color: string;
  icon: string;
}
